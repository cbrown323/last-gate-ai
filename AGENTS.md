# Last Gate AI — agent instructions

**Full rules:** [workflow_guide/AGENTS.md](workflow_guide/AGENTS.md) (read that file for conventions, stack rules, and communication protocol).

**Quick facts:** Next.js 15 App Router + TypeScript + shadcn/ui under `app/`, `components/`, `lib/`. Shared types in `types/`. Database via Prisma (`prisma/schema.prisma`). Verify every path with search before editing — do not assume a Vite or Laravel layout.
